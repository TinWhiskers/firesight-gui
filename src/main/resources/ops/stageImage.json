{
    "name" : "stageImage", 
    "description" : "Copy the image from a previous pipeline stage. This is useful for restoring the original camera image after visual processing stages have constructed the desired model. The restored image is then available for human visual feedback from later stages such as op-drawRects.",
    "parameters" : [
	    { 
	       "name":"stage",
	       "default":"",
	       "description":"The name of the stage with the input. Default is input"
	    }
    ]
}